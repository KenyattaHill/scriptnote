{
  "name": "scriptnote",
  "version": "1.0.1",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "bin": "dist/index.js",
  "scripts": {
    "start": "tsc --watch --preserveWatchOutput",
    "prepublishOnly": "esbuild src/index.ts --platform=node --outfile=dist/index.js --bundle --minify --define:process.env.NODE_ENV='production'"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@scriptnote/local-client": "^1.0.1"
  },
  "devDependencies": {
    "@scriptnote/local-api": "^1.0.1",
    "@types/node": "^14.14.41",
    "commander": "^7.2.0",
    "esbuild": "0.8.26",
    "open": "^8.0.5",
    "typescript": "^4.2.4"
  }
}
